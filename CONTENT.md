# 구름사이 화면 문구

## 메뉴

- 이동 추천
- 도시 날씨
- 구현 과정
- 수업 실습 기록

## 이동 추천

- 제목: `도착할 때 맑은 곳을 찾습니다.`
- 설명: `출발 시각과 활동을 고르면 이동 가능한 도시의 예보를 비교합니다.`
- 결과 제목: `추천 목적지`
- 지도 제목: `이동 경로`
- 지도 설명: `실제 도로 경로와 목적지 주변 건물을 함께 확인합니다.`
- 지도 보기: `이동 경로`, `전체 지역`, `비 지역`, `흐림 지역`
- 기상 지도 설명: `한국 48개 기준 지역, 144개 세부 지점의 현재 날씨를 비교합니다.`
- 기상 지도 한계: `Open-Meteo 현재 모델값을 지점별로 표시하며 기상 레이더 경계는 아닙니다.`
- 실제 날씨 검증: `네이버 날씨·기상청 레이더와 교차 확인`
- 빈 결과: `현재 확인된 지점에는 비가 없습니다.` / `현재 확인된 지점에는 흐린 곳이 없습니다.`
- 지도 안내: `3D 건물과 이동선을 함께 확인합니다.`
- 지도 모션: `날씨 모션 켬`, `날씨 모션 끔`
- 관광지 안내: `이 날씨에 가볼 곳`
- 반복되는 서비스 소개 세 블록은 삭제합니다.
- 화면 설정: `화면 효과 켬`, `화면 효과 끔`, `화면 효과 대기`
- 오버레이 상태: `도시명 · 날씨 상태`

## 구현 과정

1. `현재 날씨 카드에서 출발했습니다.`
2. `수업에서 배운 개념을 서비스 흐름에 연결했습니다.`
3. `이동 시간 → 도착 예보 → 활동 점수 → 경로 표시 순서로 처리합니다.`
4. `API 키 누락, 부분 실패, 경로 추정을 각각 화면 상태로 처리했습니다.`
5. `현재 지도는 실제 이동선, 3D 건물, 48개 지역의 144개 날씨 지점을 함께 보여 줍니다.`

구현 설명은 위 다섯 항목 안에서만 하며 같은 사실을 다른 제목으로 반복하지 않습니다.

## 수업 실습 기록

- 제목: `수업 실습 기록`
- 설명: `PDF의 코드 챌린지를 페이지 순서대로 실행해 볼 수 있습니다.`
- 각 구역은 `페이지 + 학습 주제 + 실행 화면`만 표시합니다.

## 말투

- `습니다`로 끝내되 문장을 짧게 씁니다.
- 사용자가 누를 수 없는 기능을 권유하지 않습니다.
- 기술 이름은 구현 과정과 실습 기록에서만 사용합니다.
- `최적`, `완벽`, `안전한 경로`처럼 증명하지 못한 표현은 쓰지 않습니다.
