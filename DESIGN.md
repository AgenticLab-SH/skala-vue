# 구름사이 화면 기준

## 방향

구름사이는 반복해서 조건을 바꾸는 `operate` 화면입니다. 시각적 특징은 **날씨에 따라 이동하는 경로를 실제 지도 위에서 확인하는 장면**에 둡니다. 나머지는 흰 바탕, 선, 여백, 숫자 정렬로 조용하게 구성합니다.

Apple의 Liquid Glass 원칙처럼 유리 재질은 콘텐츠 카드가 아니라 상단 탐색과 지도 조작처럼 콘텐츠 위에 떠 있는 기능 계층에만 사용합니다. CSS로 완전히 복제하려 하지 않고 투명도·흐림·얇은 테두리·짧은 눌림 반응만 적용합니다.

날씨 오버레이는 장식이 아니라 현재 보고 있는 예보의 상태 표시입니다. 비는 빗방울, 흐림은 구름층, 안개는 옅은 띠, 눈은 눈송이로 구분합니다. 이동 추천에서는 목적지 도착 예보, 도시 상세에서는 현재 예보를 따릅니다. 여러 도시가 섞인 목록에서는 한 도시로 검색 범위가 좁혀졌을 때만 표시합니다.

지도 기상 레이어는 화면 오버레이와 역할을 분리합니다. 첫 단계에서는 `이동 경로`와 `전국 날씨`만 선택하고, 전국 날씨를 연 뒤에만 `전체`, `비`, `흐림` 필터를 보여 줍니다. 비는 파란 면, 흐림은 회색 면으로 표시하되 지점 원과 도시 이름을 함께 남겨 색만으로 구분하지 않습니다.

지도 날씨 모션은 각 지점 좌표에 고정하고 이동 경로를 포함한 모든 지도 모드에서 표시합니다. 전국 배율에서는 기준 지점만 작게 보이고 확대하면 세부 지점이 나타납니다. 비는 짧고 가는 빗줄기, 흐림은 작은 구름층으로 표현해 지역 이름과 이동선을 가리지 않습니다. 지도 이동·확대 후에도 해당 지점을 따라가며, 지도 오른쪽 위의 분명한 토글로 끌 수 있습니다. 기본값은 켜짐입니다.

## 토큰과 타이포그래피

- `--ink #17202a`: 제목과 핵심 정보
- `--muted #626c77`: 보조 설명
- `--accent #1e66c1`: 선택, 링크, 경로
- `--route #2f7de1`: 지도 경로
- `--surface #ffffff`: 읽는 콘텐츠
- `--soft #f2f5f8`: 구분 배경
- `--glass rgba(255,255,255,.72)`: 탐색·지도 조작 재질
- `--radius-control 12px`: 입력과 일반 조작
- `--radius-card 16px`: 일반 정보 카드
- `--radius-panel 18px`: 대표 추천과 지도 프레임
- `--shadow-panel`: 대표 작업 영역에만 쓰는 낮은 그림자
- 최대 작업 폭 `1180px`, 구현 과정 읽기 폭 `820px`, 기능 유리 radius `999px`
- 주요 간격 `8, 12, 16, 24, 32, 48, 64px`
- 상호작용 `140ms ease`, 화면 상태 `220ms ease-out`

한글은 Pretendard, Noto Sans KR, Apple SD Gothic Neo 순서로 사용합니다. 웹 폰트는 추가하지 않습니다.

## 레이아웃과 컴포넌트

- 헤더는 스크롤 위에 떠 있는 기능 계층이며, 본문과 충분한 간격을 둡니다.
- 입력 폼은 하나의 작업 단위라 외곽선을 한 번만 사용합니다.
- 대표 추천은 활동 장소와 핵심 수치를 두 영역으로 나누고 안쪽 카드 중첩을 피합니다.
- 활동 장소는 대표 장소 한 곳과 날씨 대안 한 곳만 남깁니다. 장소 유형, 이름, 선택 이유를 한 줄 구조로 비교합니다.
- 경로 지도는 추천 바로 아래에 두며, 지도 위 조작만 유리 재질을 사용합니다.
- 지도 종류는 `이동 경로 / 전국 날씨` 두 개로 먼저 구분하고, 비·흐림 필터와 경로 방식은 선택한 지도에서만 나타나게 합니다.
- 지도 모션 토글은 지도 안 오른쪽 위에 두고 `날씨 모션 켬/끔`을 글과 스위치로 함께 표시합니다.
- 지도 바로 아래에는 `외부 날씨와 비교하기` 접힘 영역을 둡니다. 평소에는 열기 행동이 보이는 한 줄만 남기고, 펼치면 현재 중심·배율을 반영한 기상청 비교 지도와 네이버 날씨·기상청 레이더 확인 링크를 보여 줍니다.
- 활동과 무관한 일반 관광지 세 곳을 반복하지 않습니다. 활동 장소가 먼저 나오고 날씨가 나쁠 때만 일반 관광지를 대안으로 붙입니다.
- 후보는 독립 카드보다 비교 가능한 목록 행으로 두고, 데스크톱에서는 대표 추천 옆의 폭이 좁은 패널에 모두 표시합니다. 모바일에서는 대표 추천 아래로 이어지며 패널 안쪽만 과도하게 스크롤시키지 않습니다.
- 이동 경로에는 `빠른 경로`와 `그늘 경로`를 같은 수준의 선택 버튼으로 둡니다. 그늘 경로를 선택하면 목적지 3D로 자동 전환하고, 도착 시각·태양 고도·분석한 건물 수·표시 구간의 예상 그늘 비율을 한 번만 보여 줍니다.
- 그림자는 건물 바닥에서 태양 반대 방향으로 이어지는 짙은 반투명 면으로 표시합니다. 일반 목적지 3D에서도 그림자를 계산하고, 지도 안 범례에 도착 시각 기준임을 표시합니다. 3D 그림자 보기에서는 겹치는 날씨 모션을 숨깁니다.
- 접힘 영역의 제목만으로 행동을 추측하게 하지 않습니다. `외부 날씨와 비교하기` 옆에 `비교 자료 열기/닫기`를 표시하고 hover·focus·open 상태가 구분되게 합니다.
- 구현 과정은 `문제 → 배운 개념의 적용 → 데이터 흐름 → 실패 → 한계` 순서로 읽힙니다.
- 실습 기록은 서비스 스타일을 덧씌우지 않고 수업 결과가 구분되게 유지합니다.

## 접근성과 상태

- 터치 대상은 44px 이상이며 키보드 포커스가 항상 보입니다.
- 유리 배경에서도 텍스트 대비를 유지하고, `prefers-contrast`와 backdrop-filter 미지원 환경에서는 불투명 배경으로 바꿉니다.
- `prefers-reduced-motion`에서는 지도 카메라 애니메이션과 눌림 이동을 줄입니다.
- 지도는 대체 텍스트 요약과 오류 상태를 갖고, 추천 기능 자체를 막지 않습니다.
- 기상 데이터가 실패하면 이동 경로 지도는 그대로 유지하고 `현재 기상 레이어를 불러오지 못했습니다`라고 구분합니다.
- 날씨 오버레이는 `pointer-events: none`으로 조작을 가리지 않으며 상단 버튼으로 언제든 끌 수 있습니다. 설정은 Pinia와 localStorage에 남깁니다.
- 지도 모션도 `pointer-events: none`이며 별도 설정을 Pinia와 localStorage에 남깁니다.
- 네이버 날씨와 레이더 전체 화면은 새 창으로 열리며, 네이버 페이지를 삽입할 수 없다는 제한과 제공자별 갱신 시각 차이를 함께 알립니다.
- `prefers-reduced-motion`에서는 낙하·이동 애니메이션을 없애고 정적인 대기색과 구름층만 남깁니다. 고대비 모드에서는 오버레이를 숨깁니다.
- 로딩, 빈 결과, 일부 실패, 전체 실패, 추정 경로를 서로 다른 문장으로 구분합니다.

## 피할 것

- 모든 카드에 유리·블러·그림자 적용
- 의미 없는 색 막대와 장식용 그라데이션
- 둥근 카드 안에 다시 둥근 카드 중첩
- 영어 분위기 라벨, 가짜 지표, 과장된 안전 표현
- 같은 기능 설명을 홈 하단과 구현 과정에서 반복하는 것
- 제목, 설명, 이유 목록과 장소 이유가 같은 사실을 반복하는 것
