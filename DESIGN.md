# 구름사이 화면 기준

## 원칙

이 서비스는 반복해서 조건을 바꾸는 작업 화면이므로 `operate` 성격으로 봅니다. 날씨 앱에서 흔히 쓰는 큰 그라데이션이나 여러 색의 카드 대신, 종이에 이동 계획을 적고 비교하는 것처럼 선, 여백, 숫자의 정렬로 구분합니다.

한 가지 기억 요소는 **도착 시각 기준의 추천 문장과 큰 적합도 숫자**입니다. 나머지는 입력과 비교를 방해하지 않도록 조용하게 둡니다. 장식 목적으로 붙인 영문 라벨과 01·02·03 번호는 사용하지 않습니다.

## 색과 타이포그래피

- 먹색 `#17191c`: 본문과 주요 선
- 회색 `#676c73`: 보조 설명
- 파랑 `#2563a8`: 링크, 포커스, 선택 상태처럼 의미가 있는 곳
- 옅은 회색 `#f4f5f6`: 선택·비교 배경
- 흰색 `#ffffff`: 입력과 결과 배경
- 바탕 `#fbfbfa`: 전체 배경

한글은 설치 여부에 따라 Pretendard, Noto Sans KR, Apple SD Gothic Neo 순서로 사용합니다. 별도 웹 폰트를 불러오지 않아 배포 실패 지점을 늘리지 않습니다. 큰 제목은 자간과 줄 간격으로 성격을 만들고, 본문은 16px 안팎과 충분한 행간을 유지합니다.

## 레이아웃과 간격

- 최대 본문 폭: 1160px
- 데스크톱 입력: 세 열, 활동: 다섯 열
- 760px 이하: 입력 한 열, 결과 한 열
- 680px 이하: 시간 비교와 주요 메뉴 재배치
- 터치 대상: 최소 44px
- 간격은 8, 12, 16, 24, 32, 56px을 중심으로 사용

## 컴포넌트와 상태

- 입력 폼은 하나의 작업 단위라 외곽선 한 번만 사용합니다.
- 추천 결과는 설명과 숫자를 두 영역으로 나누되 안쪽에 카드를 중첩하지 않습니다.
- 후보 도시는 독립 카드보다 한 목록의 행으로 비교합니다.
- 로딩은 고정 폭 선의 `transform`만 움직이고, reduced motion에서는 위치 이동을 없앱니다.
- 빈 결과, 일부 실패, 전체 실패, 캐시된 값, 추정 경로를 각각 문장으로 구분합니다.

## 상호작용과 접근성

- 키보드 포커스는 파란 외곽선으로 항상 보입니다.
- 라디오 선택은 숨은 input의 포커스가 바깥 라벨에 표시됩니다.
- 주요 버튼은 100~160ms 안의 짧은 눌림 피드백만 사용합니다.
- hover 효과는 마우스가 있는 환경에서만 적용합니다.
- `prefers-reduced-motion`이면 스크롤 이동과 위치 기반 로딩 애니메이션을 제거합니다.
- 결과 변경은 `aria-live`로 알리고, 사용자가 직접 제출한 경우 결과 제목으로 포커스를 옮깁니다.

## 피할 것

- 이유 없는 색 막대, 그라데이션, 유리 효과
- 모든 내용을 둥근 카드로 감싸기
- 실제 순서가 아닌 장식용 번호
- 영어로 된 분위기용 라벨
- 출처가 없는 통계와 과장된 안전 표현
- 같은 내용을 헤더, 카드, 배지에서 반복하기
