# 구름사이 정보 구조

## 경로와 메뉴

| 경로 | 사용자 목적 | 주요 상태 |
| --- | --- | --- |
| `/` | 조건을 정하고 이동 추천을 받습니다. | 초기, 로딩, 성공, 빈 결과, 일부 실패, 전체 실패 |
| `/weather/search` | 도시 현재 날씨를 검색하고 관심 도시를 고릅니다. | 검색어, 관심 도시 필터, 로딩, 일부 실패, 빈 검색, 전체 실패 |
| `/weather/:cityId` | 선택 도시의 시간대별 예보를 봅니다. | 로딩, 성공, 일출·일몰 일부 실패, 날씨 실패 |
| `/about` | 데이터 흐름과 학습 과정, 제한을 확인합니다. | API 키 유무, 공개 대체 제공자 |
| `/challenges` | PDF 코드 챌린지 결과를 확인합니다. | 각 실습 컴포넌트 상태 |

1차 메뉴는 `이동 추천`, `도시 날씨`, `만든 과정`으로 유지합니다. 코드 챌린지는 제출물의 학습 근거이지만 서비스 핵심 탐색은 아니므로 footer에 둡니다.

## 핵심 흐름

```text
홈 진입
→ 저장된 출발 조건 확인
→ 활동·시간·범위 수정
→ 날씨와 이동 경로 비교
→ 대표 목적지와 이유 확인
→ 다른 도시 또는 출발 시간 비교
→ 도시 상세 확인
→ 해당 도시를 새 출발지로 선택해 다시 계획
```

사용자가 직접 비교를 실행하면 결과 제목으로 포커스를 이동합니다. 첫 진입의 자동 비교는 포커스를 움직이지 않습니다.

## 상태 소유권

- Pinia/localStorage: 온도 단위, 관심 도시, 마지막 출발 조건
- URL query: 도시 검색어, 관심 도시 필터
- 동적 URL: 상세 도시 코드
- composable: 현재 추천 요청 상태와 결과
- API service: 10분 날씨 캐시와 제공자별 응답 정리

뒤로 가기와 새로고침 후에도 검색어와 관심 도시 필터는 URL로 복원합니다. 상세에서 `이 도시로 다시 계획하기`를 누르면 Pinia의 출발 도시를 먼저 바꾸고 홈으로 이동합니다.

## 모바일 탐색

메뉴를 별도 햄버거로 숨기지 않습니다. 항목이 세 개라 두 줄 header에서 그대로 보여 주는 편이 더 빠릅니다. 입력은 한 열로 바꾸고 주요 제출 버튼은 전체 폭을 유지합니다. 후보 행은 점수와 날씨가 세로로 이어지도록 재배치합니다.

## 실패와 복귀

- 조건 안에 실제 경로가 남지 않으면 이동 시간을 늘리라는 빈 결과를 보여 줍니다.
- 일부 도시만 실패하면 성공 결과를 유지하고 제외된 수를 표시합니다.
- 날씨 전체 실패는 입력을 보존하고 같은 화면에서 다시 시도합니다.
- 경로 실패는 추정값으로 계속하되 출처를 표시합니다.
- 잘못된 도시 URL은 404로 보내고 도시 목록과 홈 복귀 경로를 제공합니다.
