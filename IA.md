# 날씨의 요정 정보 구조

## 메뉴와 경로

| 경로               | 메뉴명         | 역할                                              |
| ------------------ | -------------- | ------------------------------------------------- |
| `/`                | 이동 추천      | 조건 입력, 추천, 실제 경로, 후보와 시간 비교      |
| `/weather/search`  | 도시별 날씨    | 시·도 선택, 현재 날씨 검색과 관심 도시 관리       |
| `/process`         | 구현 기록      | 구현 이유, 수업 개념 적용, 트러블 슈팅, 최종 결과 |
| `/prompts`         | 부록 경로      | 실제 구현 방향을 정한 요청과 반영 기술            |
| `/challenges`      | 수업 실습 기록 | PDF 페이지별 코드 챌린지와 실행 결과              |
| `/reference`       | 활용자료       | 외부 API·오픈소스·지도·참고 서비스와 적용 범위    |
| `/weather/:cityId` | 문맥 경로      | 선택 도시의 예보와 일출·일몰 상세                 |

`/about`은 기존 링크를 위해 `/process`로 보냅니다. 다섯 개의 1차 메뉴는 같은 이름을 유지하며, 모바일에서는 한 줄로 억지로 줄이지 않고 가로 스크롤 가능한 탐색 영역으로 둡니다. `/prompts`는 README 하단에서만 연결하는 부록 경로입니다. 수업 실습 목차는 `#/challenges#challenge-{page}`에 페이지 상태를 남겨 새로고침과 복사한 링크에서도 같은 항목으로 이동합니다.

환영 화면은 별도 경로를 만들지 않습니다. 접속과 새로고침마다 2초 동안 표시하고 Escape나 자동 닫기 뒤 현재 경로의 본문으로 돌아갑니다. 경로·날씨 비교 애니메이션은 홈의 `loading` 상태에만 속하며, 기존 결과 위에 고정된 투명 유리 레이어로 최소 1초 동안 표시합니다.

## 핵심 흐름

```text
홈 진입 → 저장된 조건을 직접 수정하거나 빠른 설정 선택
→ 빠른 설정이면 출발 도시·내일 시각·이동 시간을 적용하고 활동 선택으로 포커스 이동
→ 활동 선택 → 추천 확인하기로 포커스 이동 → 추천 실행
→ 추천 결과로 스크롤 → 전체 후보와 도착 날씨 비교
→ 출발 도시를 포함해 해당 활동 장소가 있는 도시만 남김 → 실제 이동 시간 안의 전체 후보 비교
→ 대표 추천 옆의 전체 후보에서 원하는 곳 선택 → 대표 추천·시간 대안·경로 지도를 함께 갱신
→ 출발 도시와 후보가 같으면 행정청사에서 활동 장소까지 지역 내 경로 계산
→ 이동 경로 구간으로 스크롤 → 대한민국 전체에서 목적지 3D 건물과 그림자까지 자동 확대
→ 전체보기·확대보기 또는 드래그·터치·핀치로 지도 확인
→ 빠른 경로 또는 선택 장소 주변 그늘 경로 선택 → 3D 건물 그림자와 선택 근거 확인
→ 나침반 회전과 지도 기울기를 조절해 건물과 그림자를 원하는 각도에서 확인
→ 시간 변경 구간으로 스크롤 → 기준보다 앞뒤 세 시간의 여섯 출발 시간을 비교하고 선택한 조건으로 추천을 다시 실행
→ 전국 날씨를 연 뒤 전체·비·흐림을 골라 48개 기준 지역·144개 세부 지점 확인
→ 필요하면 외부 날씨 비교를 펼쳐 같은 중심의 기상청 지도와 네이버 날씨·레이더로 교차 확인
→ 활동 장소와 날씨가 나쁠 때의 대안 확인
→ 다른 도시·출발 시간 비교 → 도시 상세 또는 다시 계획
```

사용자가 직접 비교하면 결과 제목으로 포커스를 옮깁니다. 첫 진입 자동 비교는 포커스를 움직이지 않습니다.

## 상태 소유권

- Pinia/localStorage: 온도 단위, 관심 도시, 마지막 출발 조건
- URL query: 도시별 날씨의 검색어, 시·도 필터, 관심 도시 필터
- PlannerForm component: 빠른 설정의 내일 날짜 계산, 다음 활동·제출 조작 포커스와 일회성 하이라이트
- Pinia/localStorage: 날씨 화면 효과의 켜기/끄기
- Pinia/localStorage: 지도 날씨 모션의 켜기/끄기
- Pinia 일시 상태: 현재 화면이 표현할 도시·조건·효과 종류
- URL query: 도시 검색어, 관심 도시 필터
- 동적 URL: 상세 도시 코드
- composable: 추천 요청, 부분 실패, 추천·경로 결과
- WeatherHomeView component: 네 단계 현재 위치, 단계 이동, 추천별 지도 자동 확대 1회 여부
- API service: 날씨 캐시, OSRM GeoJSON 경로 대안, 추정 경로
- API service: OpenWeather 18개 기준 도시의 현재값을 144개 비교 지점에 연결하고 10분간 캐시
- static registry: 지역 좌표, 행정 구분, 행정청사, 일반 관광지와 활동별 장소·좌표·적합도
- RouteMap component: 지도 로딩, WebGL 오류, 전체보기·목적지 확대보기, 클릭·드래그와 모바일 터치 조작, 이동 경로·전국 날씨와 조건 필터·3D 보기, 회전·기울기, 도착 시각 태양각·건물 그림자·경로 대안 비교, 배율별 세부 모션, 검증 영역의 현재 중심 상태
- WelcomeOverlay component: 접속·새로고침마다 2초 노출과 종료 뒤 본문 포커스 복귀

## 실패와 복귀

- 실제 경로가 제한 시간을 넘으면 이동 시간을 늘리는 빈 결과를 보여 줍니다.
- 선택한 활동 장소가 이동 시간 안에 없으면 활동이나 이동 시간을 바꾸도록 안내합니다.
- 일부 도시만 실패하면 확인된 결과와 제외 수를 표시합니다.
- OpenWeather 스냅샷이 없거나 특정 도시가 누락되면 해당 기능에 오류 상태와 다시 시도 안내를 표시합니다.
- 경로 요청이 실패하면 직선거리 기반 추정선으로 계속하되 출처를 표시합니다.
- 지도 타일이나 WebGL이 실패하면 추천 정보는 유지하고 텍스트 경로 요약을 보여 줍니다.
- 그늘 계산 중에는 진행 상태를 표시하고, 해가 졌거나 건물·경로 대안이 부족하면 빠른 경로를 유지한 이유를 바로 보여 줍니다.
- 144개 표시 지점은 OpenWeather 18개 기준 도시 중 가장 가까운 도시의 현재값을 연결한 비교용 지점입니다. 관측소·기상 레이더·행정구역별 실제 강수 경계를 뜻하지 않습니다.
- 날씨 지점 데이터만 실패하면 지도는 이동 경로 보기로 남고 다시 불러오기 버튼을 제공합니다.
- 선택한 기상 조건에 해당하는 지점이 없으면 빈 지도를 보여 주지 않고 `현재 확인된 지점이 없습니다`와 전체 날씨 보기를 제공합니다.
- 날씨 효과가 꺼져 있거나 고대비·감소된 모션 환경이어도 도시명과 날씨 문구는 그대로 남습니다.
- 외부 날씨 비교 영역은 접힌 상태에서도 `비교 자료 열기` 행동을 글로 표시합니다. 네이버 날씨를 페이지 안에 띄울 수 없으면 깨진 iframe을 남기지 않고 새 창 확인 링크를 유지하며, 같은 화면 비교에는 삽입 가능한 기상청 날씨 지도를 사용합니다.
- 잘못된 도시 URL은 404로 보내고 도시 목록과 홈으로 돌아갈 수 있게 합니다.
