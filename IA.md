# 구름사이 정보 구조

## 메뉴와 경로

| 경로 | 메뉴명 | 역할 |
| --- | --- | --- |
| `/` | 이동 추천 | 조건 입력, 추천, 실제 경로, 후보와 시간 비교 |
| `/weather/search` | 도시 날씨 | 현재 날씨 검색과 관심 도시 관리 |
| `/process` | 구현 과정 | 아이디어, 수업 개념 적용, 데이터 흐름, 실패와 한계 |
| `/challenges` | 수업 실습 기록 | PDF 페이지별 코드 챌린지와 실행 결과 |
| `/weather/:cityId` | 문맥 경로 | 선택 도시의 예보와 일출·일몰 상세 |

`/about`은 기존 링크를 위해 `/process`로 보냅니다. 네 개의 1차 메뉴는 같은 이름을 유지하며, 모바일에서는 한 줄로 억지로 줄이지 않고 가로 스크롤 가능한 탐색 영역으로 둡니다.

## 핵심 흐름

```text
홈 진입 → 저장된 조건 확인 → 조건 수정 → 추천 실행
→ 대표 목적지와 이유 확인 → 실제 경로를 지도에서 확인
→ 다른 도시·출발 시간 비교 → 도시 상세 또는 다시 계획
```

사용자가 직접 비교하면 결과 제목으로 포커스를 옮깁니다. 첫 진입 자동 비교는 포커스를 움직이지 않습니다.

## 상태 소유권

- Pinia/localStorage: 온도 단위, 관심 도시, 마지막 출발 조건
- URL query: 도시 검색어, 관심 도시 필터
- 동적 URL: 상세 도시 코드
- composable: 추천 요청, 부분 실패, 추천·경로 결과
- API service: 날씨 캐시, OSRM GeoJSON, 추정 경로
- RouteMap component: 지도 로딩, WebGL 오류, 2D/3D 보기 상태

## 실패와 복귀

- 실제 경로가 제한 시간을 넘으면 이동 시간을 늘리는 빈 결과를 보여 줍니다.
- 일부 도시만 실패하면 확인된 결과와 제외 수를 표시합니다.
- 경로 요청이 실패하면 직선거리 기반 추정선으로 계속하되 출처를 표시합니다.
- 지도 타일이나 WebGL이 실패하면 추천 정보는 유지하고 텍스트 경로 요약을 보여 줍니다.
- 잘못된 도시 URL은 404로 보내고 도시 목록과 홈으로 돌아갈 수 있게 합니다.
