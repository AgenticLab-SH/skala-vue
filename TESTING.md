# 구름사이 검증 기준

## 정적 검사

```bash
npm run format
npm run lint
npm run build
npm run build:staging
npm run build:production
```

세 빌드에 개인 API 키가 없어야 하며 제품 문서 계약 검사도 통과해야 합니다.

## 브라우저 핵심 흐름

1. 같은 출발 조건에서 러닝·등산·바다·풋살·자전거를 바꾸면 추천 도시 또는 대표 활동 장소가 달라집니다.
2. 출발 도시에도 선택한 활동 장소가 있으면 후보에 포함되고, 실제 이동 시간 안의 후보가 상위 4개로 잘리지 않고 모두 표시됩니다.
3. 후보를 선택하면 대표 추천, 경로 지도와 출발 시간 비교가 같은 도시 기준으로 바뀝니다.
4. 최대 이동 시간은 30분·1시간·2시간·3시간·4시간·5시간을 선택할 수 있고, 이전 6시간 저장값은 5시간으로 안전하게 보정됩니다.
5. 바다는 해안 장소가 있는 도시만 후보에 남고, 추천 결과에는 해변 이름이 표시됩니다.
6. 활동 장소, 도착 날씨와 이동 시간을 반영한 추천과 경로 지도가 표시됩니다.
7. 날씨가 나쁠 때는 활동 장소와 별도로 실내·문화 대안 한 곳이 표시됩니다.
8. 지도에 출발지·목적지·OSRM 경로선이 보이고 목적지 3D를 열면 도착 시각 기준 건물 그림자와 범례가 표시됩니다.
9. 첫 단계에는 이동 경로·전국 날씨만 보이고, 전국 날씨를 연 뒤 전체·비·흐림 필터가 나타납니다.
10. 비·흐림 지점을 선택하면 도시명, 상태, 강수량, 운량이 표시됩니다.
11. 지도 날씨 모션은 기본으로 켜지고 지도 안 토글로 즉시 끄고 다시 켤 수 있습니다.
12. 첫 화면의 이동 경로 모드에서도 날씨 모션과 지도 안 토글이 바로 보입니다.
13. 전국 배율에서는 작은 기준 지점만 보이고 확대하면 중심권·북동권·남서권 세부 지점이 나타납니다.
14. 빠른 경로와 그늘 경로 버튼이 키보드로 선택되고, 그늘 경로는 목적지 3D로 자동 전환해 태양각·건물 높이 그림자와 선택 근거를 표시합니다.
15. 경로 대안이 있으면 예상 그늘 비율과 추가 시간을 비교해 한 경로를 선택하고, 해가 진 시각·건물 없음·대안 없음에서는 빠른 경로 유지 이유가 표시됩니다.
16. 지도 아래 `외부 날씨와 비교하기`는 기본으로 접혀 있어도 `비교 자료 열기`가 보이고, 펼치면 현재 중심·배율의 기상청 비교 지도와 네이버 날씨·기상청 레이더 링크가 나타납니다.
17. 관심 도시 필터, URL query, 온도 단위, 상세 재계획이 유지됩니다.
18. `/process`에는 구현 과정이, `/challenges`에는 PDF의 13개 주제·18개 명시 페이지가 표시됩니다.
19. 실습 목차의 각 페이지를 누르면 `#/challenges#challenge-{page}`를 유지한 채 해당 실습으로 이동합니다.
20. `/reference`에는 참고 사이트와 실제로 참고한 범위가 표시됩니다.
21. `/about`은 `/process`로 이동하고 없는 도시는 404로 이동합니다.
22. 비·흐림·안개·눈 상태가 서로 다른 오버레이로 보이고, 효과 버튼을 끄면 즉시 사라집니다.
23. 이동 추천은 목적지 도착 예보, 상세는 현재 예보, 검색은 한 도시로 좁힌 결과를 표시합니다.
24. 229쪽 Axios 실습에서 날씨 GET과 JSON GET·POST·PATCH·DELETE의 성공·실패 상태가 표시됩니다.

## 오류와 부분 성공

- 이동 제한 안에 실제 경로가 없는 빈 결과를 확인합니다.
- 일부 도시 실패에서도 확인된 결과가 남는지 확인합니다.
- OSRM 실패 시 추정선과 `직선거리 기반 추정`이 보이는지 확인합니다.
- 지도 타일 또는 WebGL 실패 시 텍스트 요약이 남는지 확인합니다.
- OpenWeather 키가 없는 production에서 Open-Meteo로 동작하는지 확인합니다.
- 기상 격자 API 실패 시 이동 경로와 추천이 남고 다시 불러오기 안내가 보이는지 확인합니다.
- 비 또는 흐린 지점이 0개일 때 빈 상태와 `전체 날씨 보기`가 동작하는지 확인합니다.

## 화면과 접근성

- 320, 390, 768, 1440px에서 가로 넘침과 겹침을 확인합니다.
- Tab으로 메뉴, 입력, 활동, 제출, 결과, 지도 조작을 이동합니다.
- 200% 확대, reduced motion, backdrop-filter 미지원 대체 스타일을 확인합니다.
- 효과 끄기 설정이 새로고침 뒤에도 유지되고 오버레이가 클릭·키보드 조작을 막지 않는지 확인합니다.
- console error/warning과 실패한 핵심 network 요청을 확인합니다.

## 배포 완료

GitHub Pages에서 홈·검색·구현 과정·수업 실습·레퍼런스·상세·404를 열고, 실제 추천과 지도를 확인한 뒤에만 배포 완료로 기록합니다.

## 2026-08-22 로컬 확인 결과

- 서울·러닝과 창원·바다 조건에서 추천 도시와 OSRM 경로가 함께 바뀌었습니다.
- 전체 경로와 목적지 3D 전환이 동작했고 새 탭의 console error/warning은 0건이었습니다.
- OpenFreeMap 요청을 차단해도 추천은 유지되고 12초 뒤 텍스트 경로 요약으로 전환되었습니다.
- `/process`, `/challenges` 13개 구역, `/about` 이전 경로를 확인했습니다.
- 320, 390, 768, 1440px에서 문서 가로 넘침이 없었습니다.
- 200% CSS 확대에서 가로 넘침이 없었고 reduced motion에서 스크롤과 전환 시간이 줄었습니다.

## 2026-08-22 날씨 효과 확인 결과

- 서울 출발 추천의 강릉 이슬비는 `rain`, 수원 한 도시 검색의 흐림은 `cloud` 효과로 표시됐습니다.
- 수원 상세를 새로 요청했을 때 이슬비로 바뀌자 구름에서 약한 비 효과로 함께 바뀌었습니다.
- 맑은 전주는 오버레이를 만들지 않았고, 구현 과정으로 이동하면 남아 있던 날씨 효과가 정리됐습니다.
- 효과를 끄면 오버레이가 즉시 DOM에서 제거됐으며 새로고침 뒤에도 꺼진 설정이 유지됐습니다.
- 오버레이의 `pointer-events`는 `none`, 설정 버튼 높이는 44px로 확인했습니다.
- 이번 브라우저 연결에서는 임시 viewport 요청이 데스크톱 폭에 적용되지 않아 모바일 시각 재검증은 보류했습니다. 320·390px 레이아웃 규칙과 이전 회귀 결과는 유지했습니다.

## 2026-08-22 기상 지도 확인 결과

- Open-Meteo 현재 모델값을 48개 기준 지역의 144개 세부 지점으로 나눠 표시했습니다.
- 전국 배율에서는 기준 지역만 표시하고 확대하면 중심권·북동권·남서권 세부 지점이 나타났습니다.
- 비·흐림 보기의 기준 지역 수, 세부 지점 수, 지역 목록 수가 서로 일치했습니다.
- 최종 확인 시점에는 비 31개 기준 지역·83개 세부 지점, 흐림 37개 기준 지역이었습니다. 날씨 값은 갱신 시점에 따라 달라집니다.
- 비와 구름 marker를 작게 줄였고 지도 확대·축소 후에도 각 지점 좌표를 따라갔습니다.
- 지도 모션은 처음 켜진 상태였고, 지도 안 토글로 끄면 지점 모션이 0개가 됐습니다. 설정은 새로고침 뒤에도 유지됐고 다시 켠 상태로 복구했습니다.
- 지도 모션 marker는 `pointer-events: none`이며 장식용 요소에 role과 tabindex가 남지 않았습니다.
- `실제 날씨 검증`은 기본으로 접혀 있었고 현재 지도 중심·배율의 기상청 비교 지도, 네이버 날씨, 기상청 레이더와 가까운 비 지점 링크를 제공했습니다.
- 확대 수준을 잘못 중첩한 MapLibre 원형 레이어 오류를 수정한 뒤 새로 불러온 화면의 console error/warning은 0건이었습니다.
- 이동 추천 선택지는 18개 도시, 지역 관광지 카탈로그는 48개 지역·144곳으로 확인했습니다.
- 제주 추천에서 맑은 날은 이호테우해변·용두암이 실내 장소보다 먼저 표시됐고, 상세 화면에도 세 곳이 이어졌습니다.
- 구현 과정과 문서에서 제외하기로 한 주제의 문구가 남지 않았습니다.
- 데스크톱 홈·도시 검색·상세 화면에 가로 넘침이 없고 브라우저 console error/warning은 0건이었습니다.

## 2026-08-22 활동별 장소 확인 결과

- 서울 출발·최대 6시간의 같은 조건에서 러닝은 창원 용지호수공원, 등산은 창원 정병산을 추천했습니다.
- 바다는 제주 이호테우해변, 풋살은 창원축구센터, 자전거는 광주 영산강 자전거길을 추천했습니다.
- 후보 카드도 각 활동의 코스·산·해변·구장·자전거길 이름으로 바뀌었습니다.
- 바다 후보에는 해안 장소가 있는 도시만 남았습니다.
- 비가 오던 서울 상세에서는 여의도한강공원과 실내 대안인 국립중앙박물관이 함께 표시됐습니다.
- 상세 URL의 활동 query를 유지했고, 데스크톱 가로 넘침과 console error/warning은 0건이었습니다.

## 2026-08-23 최종 통합 확인 결과

- 275쪽 PDF 전체에서 `Code Challenge` 표기를 다시 추출해 13개 주제와 18개 명시 페이지를 확인했습니다.
- 실습 화면의 `challenge-{page}` DOM ID가 18개인지 검사했고, 일반 `href="#challenge-..."` 링크가 남지 않았음을 확인했습니다.
- Vite SSR 환경에서 서울·러닝·최대 6시간 추천을 실행했을 때 후보 18곳이 남았고 출발지인 서울도 포함됐습니다.
- 후보를 인천으로 선택했을 때 대표 추천이 인천으로 바뀌고 3개 출발 시간 대안도 함께 다시 계산됐습니다.
- 레퍼런스 원본과 production preview의 index·favicon이 모두 HTTP 200으로 응답했습니다.
- format, lint, 기본·staging·production build와 제품 문서 계약 검사가 통과했습니다.

## 2026-08-23 이동 범위·그늘 경로 확인 결과

- 최대 이동 시간 선택지를 30분·1시간·2시간·3시간·4시간·5시간으로 확인했습니다. 서울·러닝 30분에는 서울 한 곳이 남고, 5시간에는 서울을 포함한 10곳이 남았습니다.
- 모의 건물 높이와 두 경로로 그림자를 계산했을 때 그늘 표본이 많은 두 번째 경로가 선택됐습니다.
- OSRM 공개 서버의 같은 요청에서 경로 대안 2개가 반환되는 것을 확인했습니다. Node Axios 연결이 막힌 경우에는 기존 직선거리 추정 경로 한 개로 복귀했습니다.
- 그늘 계산은 해가 진 시각, 건물 높이 없음, 표시 구간과 경로가 겹치지 않음, 경로 대안 없음 상태를 각각 다른 문장으로 처리합니다.
- 기본·staging·production 빌드, format, lint와 production preview의 진입 파일·핵심 asset HTTP 200을 확인했습니다.
- Chrome에서 낮 시간의 서울→인천 조건으로 그늘 우선을 실행해 경로 대안 2개, 태양 고도 18°, 건물 450개가 계산되고 3D 지도에 반영되는 것을 확인했습니다.
- 390px에서 문서 `scrollWidth`와 `clientWidth`가 390px로 일치했고, 대표 추천·후보 패널·경로 선택·날씨 비교 영역이 화면 안에 들어왔습니다. 경로 선택 버튼은 높이 62px, 날씨 비교 요약은 104px로 키보드·터치 대상을 확보했습니다.
- 반복 회귀 요청 중 Open-Meteo가 일부 요청에 429를 반환했지만 확인된 후보는 남았고 부분 실패 처리 외의 애플리케이션 오류는 없었습니다.

## 2026-08-23 3D 그림자·지도 조작 확인 결과

- 서울에서 수원으로 17시 출발 조건을 실행해 17시 40분 도착 기준 태양 고도 17°, 건물 450개의 그림자 면이 3D 건물 바닥에 표시되는 것을 확인했습니다.
- 그늘 경로는 경로 대안 2개를 비교해 예상 그늘 6%와 추가 시간 없음 결과를 표시했고, 일반 목적지 3D에서도 같은 도착 시각 그림자와 범례가 나타났습니다.
- 지도 첫 단계의 선택지는 이동 경로·전국 날씨 두 개이며, 전국 날씨를 연 뒤에만 전체·비·흐림 세 필터가 나타났습니다.
- 21시 출발에서는 키보드 Enter로 그늘 경로를 선택했을 때 `해가 져 있어 빠른 경로만 표시합니다` 상태가 나타나고 그림자 수치를 만들지 않았습니다.
- 390px에서 문서 폭이 390px로 일치했고 경로 버튼 높이 62px, 지도 높이 430px를 유지했습니다. 애플리케이션 런타임 오류는 없었고 반복 요청 중 Open-Meteo 429 한 건은 기존 부분 실패 처리로 남았습니다.
