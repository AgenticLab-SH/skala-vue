# 구름사이 검증 기준

## 정적 검사와 빌드

```bash
npm run format
npm run lint
npm run build
npm run build:staging
npm run build:production
```

세 빌드가 모두 통과해야 하며 공개 production 결과에 개인 API 키가 없어야 합니다.

## 브라우저 핵심 흐름

1. 서울·러닝 기본 결과가 표시됩니다.
2. 창원·바다·6시간으로 바꾸면 추천, 이동 시간, 점수가 함께 바뀝니다.
3. 관심 도시를 저장하고 `관심 도시만` 필터를 켜면 URL과 목록이 함께 바뀝니다.
4. 도시 상세에서 온도 단위를 바꾸면 전체 수치가 바뀝니다.
5. 상세의 `이 도시로 다시 계획하기`가 해당 도시를 홈 출발지로 지정합니다.
6. 없는 도시 경로가 404 화면으로 이동합니다.
7. 일출·일몰 API만 실패하면 상세 전체를 막지 않고 보조 정보 실패로 표시합니다.
8. `v-html` 비교 실습에서 사용자가 입력한 태그는 실행하지 않고 문자로 표시합니다.
9. `/challenges`의 코드 챌린지 13개가 유지됩니다.

## 오류와 부분 성공

- 이동 시간을 2시간으로 줄여 실제 경로 후보가 없어지는 조건을 확인합니다.
- 날씨 요청 하나가 실패해도 확인된 도시 결과와 제외 수가 보이는지 확인합니다.
- 실패한 도시의 상태·기온·강수확률이 모두 `확인 실패` 또는 `–`로 표시되는지 확인합니다.
- 날씨 전체 실패에서 입력이 유지되고 다시 시도 버튼이 동작하는지 확인합니다.
- 경로 API 실패 시 `직선거리 기반 추정`이 보이는지 확인합니다.
- OpenWeather 키가 없는 production에서 Open-Meteo로 동작하는지 확인합니다.

## 화면과 접근성

- 320, 390, 768, 1440px에서 가로 넘침과 겹침을 확인합니다.
- Tab으로 메뉴, 입력, 활동, 제출, 결과 링크를 순서대로 이동합니다.
- 200% 확대에서도 입력과 결과를 사용할 수 있어야 합니다.
- reduced motion에서 위치 이동 애니메이션이 없어야 합니다.
- console error/warning과 실패한 핵심 network 요청을 확인합니다.

## 배포 완료 조건

Public 저장소가 로그아웃 상태에서도 열리고, GitHub Pages URL을 새로 열어 홈·검색·상세·404·챌린지 경로를 확인한 뒤에만 배포 완료로 기록합니다.
